# Local development database values for docker-compose.yml.
DB_NAME=meet_db
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Optional: set a nonblank value to enable /admin/** endpoints.
# ADMIN_API_KEY=
