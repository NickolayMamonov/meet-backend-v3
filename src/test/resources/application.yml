app:
  admin:
    api-key: test-only-admin-key
  jwt:
    secret: test-only-jwt-signing-secret-not-for-production
  sms:
    provider: disabled
  ingestion:
    enabled: false
  timepad:
    enabled: false

spring:
  task:
    scheduling:
      enabled: false
