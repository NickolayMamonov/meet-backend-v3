spring:
  application:
    name: meet-backend

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/meet_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        enable_lazy_load_no_trans: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080

app:
  geocoder:
    enabled: true
    api-key: ${LOCATIONIQ_KEY:}
    base-url: https://eu1.locationiq.com/v1
    lang: ru

  admin:
    api-key: dev-secret

  ingestion:
    enabled: ${INGESTION_ENABLED:true}
    cron: ${INGESTION_CRON:0 0 3 * * *}
    zone: ${INGESTION_ZONE:Europe/Moscow}
    only-matching-topics: true

  jwt:
    secret: ${APP_JWT_SECRET:}
    access-token-expiration-ms: ${APP_JWT_ACCESS_TOKEN_EXPIRATION_MS:900000}
    refresh-token-expiration-days: 30

  timepad:
    enabled: true
    base-url: https://api.timepad.ru/v1
    token: ${TIMEPAD_TOKEN:}
    category-ids: [452]
    keywords:
    cities:
    page-size: 100
    max-pages: 5
    days-ahead: 180
    zone: Europe/Moscow

  otp:
    expiration-minutes: 5
    max-attempts-per-hour: 5
    fake-sms: true

  storage:
    upload-dir: ./uploads
    base-url: http://localhost:8080/media
    max-file-size: 5242880
    allowed-types: image/jpeg,image/png,image/webp

logging:
  level:
    root: INFO
    dev.whysoezzy: DEBUG
    org.hibernate.SQL: WARN

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
